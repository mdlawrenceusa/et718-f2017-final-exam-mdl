<p id="notice"><%= notice %></p>

<h1>Movies</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Cast</th>
      <th>Description</th>
      <th>Movieurl</th>
      <th>Posterurl</th>
      <th>Caption1</th>
      <th>Caption2</th>
      <th>Rating</th>
      <th>Reviews</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.cast %></td>
        <td><%= movie.description %></td>
        <td><%= movie.movieurl %></td>
        <td><%= movie.posterurl %></td>
        <td><%= movie.caption1 %></td>
        <td><%= movie.caption2 %></td>
        <td><%= movie.rating %></td>
        <td><%= movie.reviews %></td>
        <td><%= link_to 'Show', movie %></td>
        <td><%= link_to 'Edit', edit_movie_path(movie) %></td>
        <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Movie', new_movie_path %>
